$(document).on('ready', function(){

  // const $frontend = $('.frontend'),
  // //       $body = $(document.body),
  // //       $bodyHeight = $body.height();


  // // $(window).scroll(function(){
  // //   console.log($body.scrollTop());
  // //   console.log($bodyHeight)
  // //   $frontend.css({
  // //     'transform': 'rotate(' + ($body.scrollTop() / $bodyHeight * 360 * 3.5355) + 'deg)'
  // //   })
  // // })

});